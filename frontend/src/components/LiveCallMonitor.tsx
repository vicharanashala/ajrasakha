import React from 'react';
import { LiveCallMonitor as ACCLiveCallMonitor } from '../../../acc/acc-frontend/src/components/LiveCallMonitor';

export const LiveCallMonitor: React.FC = () => {
  return <ACCLiveCallMonitor />;
};

export default LiveCallMonitor;

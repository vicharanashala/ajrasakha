# Docker Compose - PostgreSQL + API
# aegra dev  -> docker compose up postgres -d  (database only)
# aegra up   -> docker compose up --build      (full stack)

services:
  postgres:
    image: pgvector/pgvector:pg18
    container_name: ajrasakha-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-ajrasakha}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-ajrasakha_secret}
      POSTGRES_DB: ${POSTGRES_DB:-ajrasakha}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-ajrasakha}"]
      interval: 5s
      timeout: 5s
      retries: 5

  ajrasakha:
    build: .
    container_name: ajrasakha-api
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-ajrasakha}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-ajrasakha_secret}
      - POSTGRES_HOST=postgres
      - POSTGRES_DB=${POSTGRES_DB:-ajrasakha}
      - AEGRA_CONFIG=aegra.json
      - AUTH_TYPE=${AUTH_TYPE:-noop}
      - PORT=${PORT:-8000}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      start_period: 10s
    volumes:
      - ./src:/app/src:ro
      - ./aegra.json:/app/aegra.json:ro

volumes:
  postgres_data:

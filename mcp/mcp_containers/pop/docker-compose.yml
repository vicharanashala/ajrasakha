version: '3.8'

services:
  pop-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pop-mcp
    ports:
      - "9002:9002"
    env_file:
      - .env
    volumes:
      - hf_cache:/app/hf_cache
    restart: unless-stopped

volumes:
  hf_cache:

// Supabase client — will be activated when Supabase is connected.
// For now, the app uses mock data from src/data/mockData.ts.
//
// To activate Supabase:
//   1. Connect Supabase in the project settings
//   2. Install: npm install @supabase/supabase-js
//   3. Set env vars: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY
//   4. Uncomment and import the client below
//
// import { createClient } from "@supabase/supabase-js";
//
// const supabaseUrl = import.meta.env.VITE_SUPABASE_URL ?? "";
// const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY ?? "";
//
// export const supabase = createClient(supabaseUrl, supabaseAnonKey);

export {};

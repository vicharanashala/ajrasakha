import {weatherContainer} from './container.js';

// No controllers or validators to export for now
export const weatherModuleControllers: Function[] = [];
export const weatherModuleValidators: Function[] = [];

// Export the container module
export const weatherContainerModules = [weatherContainer];
